source 'http://rubygems.org'

gem 'sinatra'
gem 'json'
gem 'racker'
gem 'aws-record'
gem 'rake'
gem 'rubysl-base64'
# Use Slim
gem 'html2slim'
gem 'slim-rails'

# These are the dependencies that are used only for unit tests.
group :test do
  gem "rspec"
  gem "rack-test"
end
