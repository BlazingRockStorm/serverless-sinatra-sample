source 'http://rubygems.org'

ruby '3.2.0'

gem 'sinatra'
gem 'json'
gem 'rack'
gem 'rack-contrib'
gem 'aws-record'
gem 'rake'
gem 'rubysl-base64'

# These are the dependencies that are used only for unit tests.
group :test do
  gem 'rspec'
  gem 'rack-test'
end
